def panic(s: string) none =
    log "panic: {s}"

def assert(b: bool, message: string) =
    if not b then
        panic "Assertion failed: {message}"

def toString(s: Stringer) string =
    s.toString!
