import "key"
from "math" import Vec2

type Window =
    width: float
    height: float

let w = Window{0,0}

def get_window() &Window =
    &w

def Window.capture_mouse(&self, cap: bool) =

def Window.key_down(&self, _key: uint) bool =
    false

def Window.mouse_button_down(&self, button: key.MouseButton) bool =
    false

def Window.update_inputs(&self) =

def Window.set_cursor_pos_callback(&self, f: |Vec2| -> none) =
def Window.set_key_callback(&self, f: |key.Key, key.KeyAction| -> none) =