let response = Response {
    status = 200
    content-type = "text/plain"
    body = ""
    encoding = "utf-8"
}

type Server =
    listen: |int| do none

type NodeHTTP =
    createServer: ||Request, Response| -> none| -> Server

ext server: ||Request| do Response| -> Server = "server_createServer"