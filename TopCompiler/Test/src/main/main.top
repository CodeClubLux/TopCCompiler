type Addable[T] with
    operator_add: |T| -> T

type S =
    x: int

def S.operator_add(self, other: S) S =
    S{ self.x + other.x }

def S.toString(self) string =
    "S({self.x})"

println S{ 10 } + S{ 20 }
