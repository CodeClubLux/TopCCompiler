type Point =
    x: int
    y: int

def Point.toString(&self) string =
    "Point({self.x},{self.y})"

def Point.free(&self) =
    log "freed!"

array := [0,1,2,3,4,5]

i := 0
while i < 10 do
    array.append Point{ i, 0 }
    i += 1

log array.toString!