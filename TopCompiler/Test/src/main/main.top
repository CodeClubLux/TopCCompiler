import "math"

let hcfIs30 = math.hcf |> (3 ==)
let lcmIs315 = math.lcm |> (315 ==)

def hcfAndLcm(x: int, y: int) bool =
    (hcfIs30 x, y) and (lcmIs315 x, y)

def factAndHCF(x: int, y: int) int do
    println math.primeFactors x
    println math.primeFactors y
    math.hcf x, y

    //ii

def minToHours(x: int) string =
    "{x/60}.{x%60}"

//println math.primeFactors 196
//println factAndHCF 441, 1999

println math.Fraction{ 2, 5} + math.Fraction { 1, 5 }