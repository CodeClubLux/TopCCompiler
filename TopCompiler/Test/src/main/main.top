println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])

println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
println (
    filter    _, (_ > 0)
    |> map    _, (_ ^ 2)
    |> reduce _, (+)::[int]
)([-10..10])
