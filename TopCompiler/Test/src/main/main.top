/*
type SizeT is uint //should really be unsigned long

//malloc wrapper
ext c_alloc: |SizeT| -> &none = "malloc"
ext c_free: |&none| -> none = "free"

newContext := *context
newContext.allocator = &mallocWrapper as Allocator

#pushContext newContext do
    log temporary_storage.highest.toString!

    i := 0
    while i < 10 do
        log "hey {i}"
        i += 1

    log temporary_storage.highest.toString!
*/

array := make_Array!

&array.append 10
&array.append 20

log array.join ","